%table.table.table-striped.table-hover
  %thead
    %tr
      %th
      %th Title
      %th Date

  %tbody
    -@listings.each do |listing|
      %tr
        %td
          %img(src="#{Faker::Placeholdit.image("100x100", 'jpg', 'CCCCCC', '123', 'COOL IMAGE!')}")
        %td
          = link_to listing.title, listing
        %td
          = listing.created_at